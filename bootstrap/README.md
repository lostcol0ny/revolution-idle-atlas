# Bootstrap scripts

Historical. This directory held one-off scripts that seeded the initial dataset
by hand; all have been superseded by `atlas extract`, which does the same work
as a tested, repeatable command, and removed.
